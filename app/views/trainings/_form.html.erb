<%= form_with(model: training) do |form| %>
  <% if training.errors.any? %>
    <div style="color: red">
      <h2>
        <%= pluralize(training.errors.count, "error" ) %> prohibited this training from being saved:
      </h2>

      <ul>
        <% training.errors.each do |error| %>
          <li>
            <%= error.full_message %>
          </li>
          <% end %>
      </ul>
    </div>
    <% end %>

      <div class="form-group row">
        <div class="col-md-4">
          <%= form.label :name,"Nome", style: "display: block" , class: "col-md-6 col-form-label text-md-right" %>
            <%= form.text_field :name, class: "form-control" %>
        </div>
      </div>

      <div class="form-group row">
        <div class="col-md-4">
          <%= form.label :single,"Treino Sozinho?", style: "display: block" ,
            class: "col-md-6 col-form-label text-md-right" %>
            <%= form.check_box :single %>
        </div>
      </div>

      <div class="form-group row">
        <div class="col-md-2">
          <%= form.label :durantion_minutes,"Duração (minutos)", style: "display: block" ,
            class: "col-md-6 col-form-label text-md-right" %>
            <%= form.number_field :durantion_minutes, class: "form-control" %>
        </div>
      </div>
      </br>
      <div>
        <%= form.submit "Salvar" , class: "btn btn-primary" %>
      </div>

      <% end %>